---
- name: Patch Ubuntu container
  hosts: ubuntu_container
  become: yes
  tasks:
    - name: Update APT cache
      apt:
        update_cache: yes

    - name: Upgrade all packages
      apt:
        upgrade: dist

